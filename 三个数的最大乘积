class Solution {
public:
    int maximumProduct(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int m=nums[0]*nums[1];
        int n=nums.size();
        return max(m*nums[n-1],nums[n-1]*nums[n-2]*nums[n-3]);
    }
};