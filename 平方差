#include<bits/stdc++.h>
using namespace std;
int f(int x)
{
    if(x==0)return 0;
    return (x+1)/2;
}
int g(int x)
{
    return x/4;
}
int main()
{
    int L,R;
    cin>>L>>R;
    cout<<f(R)-f(L-1)+g(R)-g(L-1)<<endl;
    return 0;
}