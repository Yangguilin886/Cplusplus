#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n=0;
    cin>>n;
    vector<int> ret;
    for(int i=0;i<n;i++)
    {
        int a=0;
        cin>>a;
        ret.push_back(a);
    }
    int x=0,y=0;
    for(int i=1;i<n;i++)
    {
        if(ret[0]>0)
        {
            if(abs(ret[i])>abs(ret[0]))
            {
                if(ret[i]<0)y++;
            }
            else
            {
                if(ret[i]>0)x++;
            }
        }
        else
        {
            if(abs(ret[i])>abs(ret[0]))
            {
                if(ret[i]<0)y++;
            }
            else
            {
                if(ret[i]>0)x++;
            }
        }
    }
    int m=0;
    if(ret[0]>0)
    {
        if(y==0)cout<<1<<endl;
        else cout<<x+y+1<<endl;
    }
    else
    {
        if(x==0)cout<<1<<endl;
        else cout<<x+y+1<<endl;
    }
    return 0;
}