class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> ret;
        for(int i=0;i<nums.size();i++)
        {
            int x=nums[i];
            ret.push_back(x*x);
        }
        sort(ret.begin(),ret.end());
        return ret;
    }
};