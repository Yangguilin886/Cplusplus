class Solution {
public:
    int findGCD(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int n=nums.size();
        int x=nums[0],y=nums[n-1];
        for(int i=x;i>=1;i--)
        {
            if(x%i==0&&y%i==0)return i;
        }
        return 0;
    }
};