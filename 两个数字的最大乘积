class Solution {
public:
    int maxProduct(int n) {
        vector<int> dp;
        while(n>0)
        {
            dp.push_back(n%10);
            n/=10;
        }
        sort(dp.begin(),dp.end());
        int m=dp.size();
        return dp[m-1]*dp[m-2];
    }
};