class Solution {
public:
    int hammingDistance(int x, int y) {
        int n=x^y;
        int ret=0;
        while(n>0)
        {
            ret++;
            n&=(n-1);
        }
        return ret;
    }
};