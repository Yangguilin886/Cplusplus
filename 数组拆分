class Solution {
public:
    int arrayPairSum(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int flag=0;
        vector<int>::iterator it=nums.begin();
        while(it!=nums.end())
        {
            flag+=*it;
            it+=2;
        }
        return flag;
    }
};