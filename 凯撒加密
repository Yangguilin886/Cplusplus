#include <bits/stdc++.h>
using namespace std;
int main()
{
  string s;
  cin>>s;
  for(int i=0;i<s.size();i++)
  {
    int n=(int)s[i];
    n+=3;
    if(n>122)s[i]='a'+(n-123);
    else s[i]+=3;
  }
  cout<<s<<endl;
  return 0;
}